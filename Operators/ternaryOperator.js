let authenticated = false;

authenticated ? console.log("SIGNOUT") : console.log("SIGNIN"); // If authenticated has value "true", statement before the colon and after the question mark will run. If the value inside "authenticated" is false, last statement after the colon will run.
