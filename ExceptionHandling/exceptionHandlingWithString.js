try {
    let str = "JavaScript is fun to use"

    if(str.startsWith("JavaScript")) {
        throw "Its not very confusing"
    }
} catch (error) {
    console.log("JavaScript is a good language.");
}
